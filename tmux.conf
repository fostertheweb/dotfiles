# remap prefix to Control + a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# allow switching panes with mouse click
setw -g mode-mouse on
set-option -g mouse-select-pane on

# source conf with prefix-r
bind-key r source-file ~/.tmux.conf

# prevent auto renaming of windows
set-option -g allow-rename off

# improve colors
set -g default-terminal 'screen-256color'

# act like vim
setw -g mode-keys vi
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind-key -r C-h select-window -t :-
bind-key -r C-l select-window -t :+

# start window numbers at 1 to match keyboard order with tmux window order
set -g base-index 1
set-window-option -g pane-base-index 1

# renumber windows sequentially after closing any of them
set -g renumber-windows on

# status bar appearance
set -g status-bg '#3F3F3F'
set -g status-fg '#A7AEAE'
#set -g status-left '#[bg=#BCD92A,fg=#006E0E]#S'

# window
#setw -g window-status-bg '#3F3F3F'
#setw -g window-status-fg '#A7AEAE'
#setw -g window-status-current-bg '#D9D9D9'
#setw -g window-status-current-fg '#757575'
#setw -g window-status-activity-bg '#A9170B'
#setw -g window-status-activity-fg '#FF9925'
#
## activity
#setw -g monitor-activity on
#set -g visual-activity on
#
## message color
#set-option -g message-fg '#A9170B'
#set-option -g message-bg '#FF9925'

